# This py file will illustrae some common data pipeline architectures - EL, ETL, ETL

"""The code blocks will illustrate how data flows and is transformed in these pipeline patterns"""


"""We will begin with the most common pipeline pattern EL, which is extract and load


# code block
import pandas as pd
import numpy as np
# The data to be moved shall be read from local directory file system
 df = pd.read_